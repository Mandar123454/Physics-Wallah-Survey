# Artifacts

This folder contains analysis outputs generated by code:

- `figures/` — PNG images of key distributions and counts
- `summary.md` — A compact markdown summary of key metrics (basic stats, NPS-style score, sentiment)

To reproduce, run:

```powershell
# From repository root
pip install -r requirements.txt
python .\scripts\clean_and_analyze.py
```
